{
  "name": "nextify-middleware",
  "version": "0.1.0",
  "description": "express middleware for generating *nextjs-like* filesystem based router with typescript handlers, handlebars templates and htmx",
  "main": "dist/index.js",
  "scripts": {
    "test": "npm link \"nextify-middleware\" && jest",
    "build": "rimraf dist && tsc --project ./tsconfig.json && ls dist && cp default/**.hbs dist/default"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "handlebars": "^4.7.8",
    "handlebars-helpers": "^0.10.0",
    "rimraf": "^5.0.5"
  },
  "peerDependencies": {
    "express": "^4.18.2"
  },
  "devDependencies": {
    "@types/express": "^4.17.21",
    "@types/jest": "^29.5.11",
    "@types/node": "^20.10.5",
    "@types/supertest": "^6.0.1",
    "jest": "^29.7.0",
    "supertest": "^6.3.3",
    "ts-jest": "^29.1.1",
    "typescript": "^5.3.3"
  }
}
